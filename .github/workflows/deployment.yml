apiVersion: apps/v1
kind: Deployment
metadata:
  name: shenanigan-server
  labels:
    app: shenanigan-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shenanigan-server
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: shenanigan-server
    spec:
      containers:
      - name: shenanigan-server
        image: <IMAGE>
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: 100m
          limits:
            cpu: 100m
---
apiVersion: v1
kind: Service
metadata:
  name: shenanigan-server-service
spec:
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: shenanigan-server

