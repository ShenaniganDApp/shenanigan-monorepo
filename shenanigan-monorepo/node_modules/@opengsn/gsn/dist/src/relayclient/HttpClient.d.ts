import { PrefixedHexString } from 'ethereumjs-tx';
import PingResponse from '../common/PingResponse';
import HttpWrapper from './HttpWrapper';
import TmpRelayTransactionJsonRequest from './types/TmpRelayTransactionJsonRequest';
import { GSNConfig } from './GSNConfigurator';
export default class HttpClient {
    private readonly httpWrapper;
    private readonly config;
    constructor(httpWrapper: HttpWrapper, config: GSNConfig);
    getPingResponse(relayUrl: string): Promise<PingResponse>;
    relayTransaction(relayUrl: string, request: TmpRelayTransactionJsonRequest): Promise<PrefixedHexString>;
}
