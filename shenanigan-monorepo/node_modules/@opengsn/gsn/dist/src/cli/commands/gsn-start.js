"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const commander_1 = __importDefault(require("commander"));
const utils_1 = require("../utils");
const GsnTestEnvironment_1 = require("../../relayclient/GsnTestEnvironment");
utils_1.gsnCommander(['n'])
    .option('-w, --workdir <directory>', 'relative work directory (defaults to build/gsn/)', 'build/gsn')
    .parse(process.argv);
(async () => {
    try {
        const network = commander_1.default.network;
        const env = await GsnTestEnvironment_1.GsnTestEnvironment.startGsn(network);
        utils_1.saveDeployment(env.deploymentResult, commander_1.default.workdir);
        utils_1.showDeployment(env.deploymentResult, 'GSN started');
        console.log(`Relay is active, URL = ${env.relayUrl} . Press Ctrl-C to abort`);
    }
    catch (e) {
        console.error(e);
    }
})().catch(reason => {
    console.error(reason);
    process.exit(1);
});
//# sourceMappingURL=gsn-start.js.map