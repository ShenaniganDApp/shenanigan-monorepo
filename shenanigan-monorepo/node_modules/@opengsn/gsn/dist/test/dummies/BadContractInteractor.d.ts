/// <reference types="@openeth/truffle-typings" />
import ContractInteractor from '../../src/relayclient/ContractInteractor';
import RelayRequest from '../../src/common/EIP712/RelayRequest';
import { GSNConfig } from '../../src/relayclient/GSNConfigurator';
import { TransactionReceipt } from 'web3-core';
export default class BadContractInteractor extends ContractInteractor {
    static readonly message = "This is not the contract you are looking for";
    static readonly wrongNonceMessage = "the tx doesn't have the correct nonce";
    private readonly failValidateARC;
    constructor(provider: provider, config: GSNConfig, failValidateARC: boolean);
    validateAcceptRelayCall(relayRequest: RelayRequest, signature: string, approvalData: string): Promise<{
        paymasterAccepted: boolean;
        returnValue: string;
        reverted: boolean;
    }>;
    sendSignedTransaction(rawTx: string): Promise<TransactionReceipt>;
}
